from parser import Parser


def run(data, output_path):
    # parser = Parser(3)
    # init_data = parser.get_hdf5_file()
    # solver = Solver(init_data=init_data)
    # result_data = solver.run_service()

    # handler = Handler(result_data=result_data, output_path=output_path)
    # handler.get_graphic_output(parser.get_graphic_data())


# if __name__ == "__main__":
#     run(data= "./test/data.json", output_path="./test/result_solv_data")